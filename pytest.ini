[pytest]
testpaths = tests
markers =
    unit: Unit tests (no Flask, no external calls)
    integration: Integration tests (Flask test client, mocked external APIs)
addopts = --cov=app --cov=backend --cov-report=term-missing -q
